- script: conda env create --quiet --file .azure/environment.yml
  displayName: Create Anaconda environment

- script: |
  conda activate irisbuild
  python -m unittest discover --verbose
  displayName: Unit tests 